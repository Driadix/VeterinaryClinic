<Window x:Class="Client.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="220" Width="350"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Логин:"/>
        <TextBox x:Name="UsernameTextBox" Grid.Row="1" Margin="0,0,0,10"/>

        <Label Content="Пароль:" Grid.Row="2" Margin="0,10,0,0"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="2" Margin="0,30,0,0"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Name="RegisterButton" Content="Регистрация" Width="100" Click="RegisterButton_Click"/>
            <Button Name="LoginButton" Content="Вход" Width="100" Margin="10,0,0,0" IsDefault="True" Click="LoginButton_Click"/>
        </StackPanel>
    </Grid>
</Window>